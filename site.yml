---
# file: site.yml

- hosts: all
  sudo: true
  roles:
    - {role: common, tags: ['common', 'desktop', 'macbook', 'htpc']}
    - {role: common-workstation, tags: ['desktop', 'macbook']}
    - {role: desktop, tags: desktop}
    - {role: macbook, tags: macbook}
    - {role: htpc, tags: htpc}
